<resources>

    <style name="ModifiedTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <item name="android:statusBarColor">@color/StatusBar</item>
        <item name="colorPrimary">@color/Line</item>
        <item name="android:colorBackground">@color/LayoutBG</item>
        <item name="android:navigationBarColor">@android:color/black</item>
        <item name="colorPrimaryDark">@color/StatusBar</item>
        <item name="android:colorForeground">@android:color/background_light</item>
        <item name="android:windowBackground">@color/LayoutBG</item>
    </style>

    <style name="ModifiedTheme.Dialog.Alert" parent="Theme.AppCompat.Dialog.Alert">
        <item name="android:statusBarColor">@color/StatusBar</item>
        <item name="colorPrimaryDark">@color/StatusBar</item>
        <item name="android:colorBackground">@color/LayoutBG</item>
    </style>
</resources>
